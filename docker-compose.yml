services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    container_name: yoguide-postgres
    network_mode: host
    environment:
      POSTGRES_USER: yoguide
      POSTGRES_PASSWORD: yoguide
      POSTGRES_DB: yoguide
    volumes:
      - yoguide-pg:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U yoguide"]
      interval: 5s
      timeout: 5s
      retries: 10

  app:
    build: .
    restart: unless-stopped
    container_name: yoguide-app
    ports:
      - "3031:3031"
    environment:
      - NODE_ENV=production
      - PORT=3031  # Changed from 3030 to 3031
      - DATABASE_URL=postgresql://yoguide:yoguide@host.docker.internal:5433/yoguide?schema=public
      - DIRECT_URL=postgresql://yoguide:yoguide@host.docker.internal:5433/yoguide?schema=public
      - JWT_SECRET=change-me-to-a-long-random-string
      - JWT_EXPIRES_IN=30d
      - GOOGLE_CLIENT_ID=144840489403-kkh8tc7ral09rmeqevec56cf4m361t7p.apps.googleusercontent.com
      - FRONTEND_URL=yoguide://reset-password
      - GMAIL_USER=yoGuideafrica@gmail.com
      - GMAIL_APP_PASSWORD=umbwirenyiguhe
    depends_on:
      postgres:
        condition: service_healthy

volumes:
  yoguide-pg:
